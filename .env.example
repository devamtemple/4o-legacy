# LOCAL DEVELOPMENT — these values come from `npx supabase start`
# Copy this file to .env.local and fill in the values

# Supabase (local — from `npx supabase status`)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<from supabase start output>
SUPABASE_SERVICE_ROLE_KEY=<from supabase start output>

# Site URL (local dev)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Internal secret for AI moderation (any random string for local)
MODERATE_SECRET=local-dev-secret

# Gemini API key (optional locally — moderation skipped without it)
# GEMINI_API_KEY=

# Stripe (test keys from https://dashboard.stripe.com/apikeys)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
